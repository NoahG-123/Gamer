Person images are intentionally absent. Drop files at the exact paths named in
`content/assets.json` (e.g. `owner.png`, `contact-placeholder-a.png`) and they
appear automatically; nothing in the code depends on their contents.
